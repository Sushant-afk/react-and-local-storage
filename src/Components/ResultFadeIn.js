import { keyframes } from 'styled-components';

export const ResultFade = keyframes`
  to {
    opacity: 1;
    visibility: visible;
    top: 0;
  }
`;

export const IntroAnimate = keyframes`
   to {
       opacity:1;
       top:0px
   }
`;
